# ~/ansible-media-stack/inventory.ini

[ucore_vm]
192.168.50.135

# Optional: Define connection variables if needed
[ucore_vm:vars]
# ansible_user=your_ssh_user # If not connecting as current user or root initially
# ansible_ssh_private_key_file=/path/to/your/private_key # If using key auth
# ansible_become_password={{ vault_sudo_password }} # If sudo requires a password (Store in Vault!)
